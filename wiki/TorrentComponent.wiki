#interface of TorrentParser.

= Introduction =

提供种子中的annouce和files等信息的解析,返回格式定义。


= Details =

{{{
class TorrentComponent extends Object {
    function parse_file($file_name){
       // some codes
       return array();
    }
}
}}}


array结构：

array['announce']

array['announce-list']

array['encoding']

array['files']

array['private']

array['info_hash']

array['publisher']

array['publisher-url']